[project]
name = "agentic-ai"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "ipykernel>=7.1.0",
    "pandas-stubs~=2.3.3",
    "ruff>=0.14.10",
    "uvicorn>=0.40.0",
]

[tool.uv.workspace]
members = ["backend", "infra"]
