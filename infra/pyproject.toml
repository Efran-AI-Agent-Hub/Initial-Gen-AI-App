# uv add --package  infra

[project]
name = "infra"
version = "0.1.0"
description = "Infrastructure setup and RAG utilities"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
dev-dependencies = [
    "pytest>=8.3.0",
]
